require('dotenv/config');
const path = require('path');

// Export config using CommonJS for Prisma CLI compatibility
module.exports = {
  prisma: {
    // Point to the directory that contains the schema .prisma files
    schema: path.join(__dirname, 'src', 'prisma', 'schema'),
  },
};
